# 사주 분석 엔진 (Fortune Engine)

> 30년 명리학 거장이 인정한 AI 사주 분석 서비스의 핵심 엔진

---

## 📐 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                        3-Layer Architecture                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [Client Layer]     Next.js 14 (App Router)                     │
│  ─────────────────────────────────────────────────────────────  │
│  - React Components (src/components/)                           │
│  - Zustand Store (src/stores/)                                  │
│  - TanStack Query (캐싱/폴링)                                    │
│                                                                  │
│  [API Layer]        Next.js API Routes + Python FastAPI         │
│  ─────────────────────────────────────────────────────────────  │
│  - /api/analysis/*  → Python 비동기 파이프라인                    │
│  - /api/user/*      → Supabase Auth/Credits                     │
│                                                                  │
│  [Service Layer]    Python Backend (Railway)                    │
│  ─────────────────────────────────────────────────────────────  │
│  - ManseryeokEngine → 만세력 계산                                │
│  - GeminiService    → AI 분석 (gemini-3.0-preview)              │
│  - PromptBuilder    → 다국어 프롬프트 생성                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 분석 파이프라인

3가지 분석 서비스 제공:

| 서비스 | 단계 | 크레딧 | 상세 문서 |
|--------|------|--------|----------|
| **리포트 분석** | 11단계 | 70C | [report_analysis.md](./report_analysis.md) |
| **신년 분석** | 9단계 | 50C | [yearly_analysis.md](./yearly_analysis.md) |
| **상담 AI** | 2단계 | 10C/질문 | [consultation.md](./consultation.md) |

**공통 특징**:
- 비동기 Job Store 패턴 (폴링 기반)
- Gemini 단계별 3회 재시도
- Supabase DB 중간 저장 (크래시 복구)

---

## ⚙️ 만세력 엔진

**파일**: `python/manseryeok/engine.py`

### 사주 팔자 계산

| 기둥 | 기준 | 규칙 |
|------|------|------|
| 연주(年柱) | 입춘(立春) | 입춘 전 출생 → 전년도 간지 |
| 월주(月柱) | 절입(節入) 시각 | 24절기 기준 |
| 일주(日柱) | 자시(子時, 23:00) | 23:00 이후 → 다음날 일주 |
| 시주(時柱) | 12시진 | 2시간 단위 지지 배정 |

### 대운 계산

```python
def calculate_daewun(gender, year_stem):
    """
    양남음녀: 순행 (월주 다음 간지로)
    음남양녀: 역행 (월주 이전 간지로)
    """
```

**DaewunItem 필드**:

| 필드 | 타입 | 설명 |
|------|------|------|
| `age` | int | 시작 나이 |
| `endAge` | int | 종료 나이 (age + 9) |
| `stem` | str | 천간 |
| `branch` | str | 지지 |
| `tenGod` | str | 십신 (일간 기준) |
| `tenGodType` | str | 십신 유형 (비겁운/식상운/재성운/관성운/인성운) |
| `favorablePercent` | int | 순풍운 비율 (AI 분석) |
| `description` | str | 대운 설명 (AI 분석) |

### 지장간 (支藏干)

| 지지 | 지장간 (여기/중기/정기) |
|------|------------------------|
| 子 | 癸 |
| 丑 | 癸, 辛, 己 |
| 寅 | 戊, 丙, 甲 |
| 卯 | 乙 |
| 辰 | 乙, 癸, 戊 |
| 巳 | 戊, 庚, 丙 |
| 午 | 己, 丁 |
| 未 | 丁, 乙, 己 |
| 申 | 己, 壬, 庚 |
| 酉 | 辛 |
| 戌 | 辛, 丁, 戊 |
| 亥 | 戊, 甲, 壬 |

### API 엔드포인트

```
POST /api/manseryeok/calculate

Request:
{
  "birthDatetime": "1990-05-15T14:30:00",
  "timezone": "GMT+9",
  "isLunar": false,
  "gender": "male"
}

Response:
{
  "pillars": { "year": {...}, "month": {...}, "day": {...}, "hour": {...} },
  "daewun": [...],
  "jijanggan": { "year": [...], "month": [...], "day": [...], "hour": [...] }
}
```

---

## 🧮 점수 시스템

**파일**: `src/lib/score/`

### 점수 계산 공식 (v2.1)

```typescript
// 1. 십신 추출 (가중치 적용)
const tenGodCounts = extractTenGods(pillars, jijanggan);
// - 천간: 가중치 1.0
// - 지장간 정기: 가중치 1.0
// - 지장간 여기/중기: 가중치 0.3

// 2. 특성 점수 계산
let score = 50;  // 기본점수
for (tenGod in counts) {
  score += TRAIT_MODIFIERS[trait][tenGod] * counts[tenGod];
}
return clamp(score, 0, 100);
```

**modifier 범위**: max ±11

### 십신(十神) 정의

| 십신 | 관계 | 성격 특성 |
|------|------|----------|
| 비견 | 같은 오행, 같은 음양 | 독립심, 경쟁심, 자존심 |
| 겁재 | 같은 오행, 다른 음양 | 추진력, 승부욕, 결단력 |
| 식신 | 내가 생하는 오행, 같은 음양 | 표현력, 여유, 낙천성 |
| 상관 | 내가 생하는 오행, 다른 음양 | 창의성, 반항, 예리함 |
| 정재 | 내가 극하는 오행, 다른 음양 | 안정, 성실, 근면 |
| 편재 | 내가 극하는 오행, 같은 음양 | 사교성, 투자, 변동 |
| 정관 | 나를 극하는 오행, 다른 음양 | 책임감, 명예, 원칙 |
| 편관 | 나를 극하는 오행, 같은 음양 | 권력, 카리스마, 결단 |
| 정인 | 나를 생하는 오행, 다른 음양 | 학습, 인내, 포용력 |
| 편인 | 나를 생하는 오행, 같은 음양 | 직관, 독창성, 전문성 |

### 35개 특성 항목

| 카테고리 | 항목 |
|---------|------|
| 성격 (10) | 의지력, 사교성, 인내력, 독립심, 신뢰성, 배려심, 유머감각, 협동심, 표현력, 성실도 |
| 업무 (5) | 기획력, 추진력, 실행력, 완성도, 관리력 |
| 적성 (10) | 분석력, 협동심, 학습력, 창의력, 예술성, 표현력, 활동성, 도전정신, 사업감각, 신뢰성 |
| 연애 (10) | 배려심, 유머감각, 감성, 자존감, 모험심, 성실도, 사교성, 경제관념, 신뢰성, 표현력 |

---

## 🤖 Gemini AI 설정

**파일**: `python/services/gemini.py`

```python
self.model = genai.GenerativeModel(
    model_name="gemini-3.0-preview",
    generation_config={
        "temperature": 0.7,
        "top_p": 0.95,
        "max_output_tokens": 8192,
    }
)
```

### 프롬프트 시스템

**파일**: `python/prompts/builder.py`

```
프롬프트 빌드 파이프라인:
[마스터 페르소나] → [자평진전] → [궁통보감] → [The Destiny Code] → [로케일] → [스키마]
```

**다국어 지원**: ko, en, ja, zh-CN, zh-TW (5개 언어)

### 고전 명리학 모듈

| 모듈 | 파일 | 내용 |
|------|------|------|
| 자평진전 | `classics/ziping.py` | 용신 5원칙, 8격, 십신 |
| 궁통보감 | `classics/qiongtong.py` | 조후론, 월령 용신 |
| 조후 매트릭스 | `classics/qiongtong_matrix.py` | 10일간×12월 (120개) |
| The Destiny Code | `western/destiny_code.py` | 서구권 현대화 프레임 |

---

## 🎨 시각화 시스템

**파일**: `python/visualization/visualizer.py`

### 사주 명반 이미지

- 크기: 800x400px
- 출력: Base64 PNG
- API: `POST /api/visualization/pillar`

### 오행 색상

| 오행 | 배경색 | 텍스트색 |
|------|--------|---------|
| 木 | `#4ade80` | `#1a1a1a` |
| 火 | `#ef4444` | `#ffffff` |
| 土 | `#f59e0b` | `#1a1a1a` |
| 金 | `#e5e7eb` | `#1a1a1a` |
| 水 | `#1e3a8a` | `#ffffff` |

### UI 컴포넌트

| 컴포넌트 | 파일 | 설명 |
|---------|------|------|
| `FavorableBar` | `src/components/report/` | 순풍/역풍 비율 바 |
| `DaewunDetailSection` | `src/components/report/` | 대운 상세 카드 |
| `ElementRelationGraph` | `src/components/analysis/` | 오행 상생상극 관계도 |
| `PillarDetailModal` | `src/components/analysis/` | 기둥 클릭 상세 모달 |
| `BasicAnalysisSection` | `src/components/report/` | 기본 분석 (일간/격국/용신) |
| `DetailedScoresSection` | `src/components/report/` | 세부 점수 레이더 차트 |

### 리포트 섹션 확장 데이터 (v2.3)

각 섹션은 `extended` prop으로 상세 데이터 표시:

| 섹션 | 확장 데이터 |
|------|-----------|
| `SajuTable` | 지장간 (여기/중기/정기) 표시 |
| `BasicAnalysisSection` | 일간 특성, 격국, 용신/기신 + 추론 근거 |
| `AptitudeSection` | talents basis/level, avoidFields, talentUsage, recommendedFields suitability |
| `RomanceSection` | style, idealPartner, warnings, compatibilityPoints, loveAdvice |
| `WealthSection` | pattern, strengths, risks, advice |
| `PersonalitySection` | socialStyleDetail (type/strengths/weaknesses) |
| `DetailedScoresSection` | 연애 10항목 + 업무 5항목 레이더 차트 (Recharts)

---

## 📋 타입 시스템

### Python → TypeScript 자동 생성

```bash
npm run generate:types
```

**흐름**:
```
Pydantic (Python) → OpenAPI JSON → TypeScript (generated.d.ts)
```

**생성 파일**: `src/types/generated.d.ts`

```typescript
import { Pillars, DaewunItem, Jijanggan, CalculateResponse } from '@/types';
```

### 파이프라인 타입

**파일**: `src/lib/ai/pipeline/types.ts`

```typescript
export const PIPELINE_STEPS: PipelineStep[] = [
  'manseryeok', 'jijanggan', 'basic_analysis',
  'personality', 'aptitude', 'fortune',
  'scoring', 'visualization', 'saving', 'complete'
];

export const DEFAULT_STEP_TIMEOUTS: Record<PipelineStep, number> = {
  manseryeok: 300000,     // 5분
  basic_analysis: 300000, // 5분 (Gemini)
  scoring: 60000,         // 1분
  // ...
};
```

---

## 📁 폴더 구조

```
python/
├── main.py                 # FastAPI 엔트리포인트
├── manseryeok/             # 만세력 엔진
├── prompts/                # AI 프롬프트 (5개 언어)
│   ├── classics/           # 자평진전, 궁통보감
│   └── western/            # The Destiny Code
├── scoring/                # 점수 계산
├── services/               # 비동기 서비스
│   ├── report_analysis.py  # 리포트 분석
│   ├── yearly_analysis.py  # 신년 분석
│   └── consultation_service.py
├── schemas/                # Pydantic 모델
└── visualization/          # 이미지 생성

src/lib/
├── ai/pipeline/            # TypeScript 파이프라인
│   ├── index.ts            # AnalysisPipeline
│   ├── context.ts          # PipelineContext
│   └── step-executor.ts    # StepExecutor
└── score/                  # 십신 점수 계산
```

---

## 📚 참고 자료

### 명리학 용어

| 용어 | 한자 | 영문 | 설명 |
|------|------|------|------|
| 일간 | 日干 | Day Master | 사주의 중심, 나 자신 |
| 격국 | 格局 | Structure | 사주의 유형/패턴 |
| 용신 | 用神 | Useful God | 사주를 보완하는 오행 |
| 대운 | 大運 | Luck Cycle | 10년 단위 운의 흐름 |
| 세운 | 歲運 | Annual Luck | 연도별 운세 |

### 외부 링크

- [Gemini API Docs](https://ai.google.dev/docs)
- [Supabase Docs](https://supabase.com/docs)

---

## 📝 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|----------|
| 2026-01-07 | v2.3 | 리포트 UI 확장 (지장간/기본분석/레이더차트/섹션별 extended 데이터) |
| 2026-01-07 | v2.2 | 문서 4개 분리 (report/yearly/consultation) |
| 2026-01-06 | v2.1 | 상담 프롬프트 개선 (재질문 금지) |
| 2026-01-05 | v2.0 | 비동기 파이프라인, Job Store 패턴 |
| 2026-01-05 | v1.31 | 크레딧 가격 변경 |
| 2026-01-05 | v1.30 | Crisp 실시간 채팅 도입 |

---

**최종 수정**: 2026-01-07
